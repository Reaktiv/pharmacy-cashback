aiofiles==24.1.0
aiogram==3.15.0
aiohappyeyeballs==2.7.1
aiohttp==3.10.11
aiosignal==1.4.0
amqp==5.3.1
annotated-types==0.8.0
anyio==4.14.2
asgiref==3.12.1
attrs==26.1.0
billiard==4.2.4
celery==5.4.0
celery-types==0.26.0
certifi==2026.7.22
cffi==2.1.0
charset-normalizer==3.5.1
click==8.4.2
click-didyoumean==0.3.1
click-plugins==1.1.1.2
click-repl==0.3.0
contourpy==1.3.3
cryptography==42.0.8
cycler==0.12.1
Django==5.0.14
django-environ==0.11.2
django-stubs==6.0.7
django-stubs-ext==6.0.7
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
djangorestframework-stubs==3.17.1
filelock==3.32.3
fonttools==4.63.0
frozenlist==1.8.0
fsspec==2026.7.0
greenlet==3.5.5
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
Jinja2==3.1.6
kiwisolver==1.5.0
kombu==5.6.2
magic-filter==1.0.12
MarkupSafe==3.0.3
matplotlib==3.11.1
mpmath==1.3.0
multidict==6.7.1
mypy==1.10.1
mypy_extensions==1.1.0
networkx==3.6.1
numpy==2.4.6
nvidia-ml-py==13.610.43
# opencv-python, not -headless: ultralytics and qreader both hard-require
# the package name "opencv-python" (see requirements.txt's own history —
# pip has no way to substitute -headless for a transitive dependency named
# opencv-python; installing both is worse, not safer, since they share the
# same "cv2" import path and whichever installs last silently overwrites
# the other in site-packages). apps/bot/qr.py's own code has no direct cv2
# import — this is here purely to satisfy qreader/qrdet/ultralytics
# internally. See backend/Dockerfile for the libgl1/libglib2.0-0 system
# packages this build needs at runtime (verified via ldd, not GTK/Qt —
# those ship bundled inside the wheel).
opencv-python==5.0.0.93
packaging==26.2
Pillow==12.3.0
pillow-heif==1.5.0
playwright==1.62.0
pluggy==1.6.0
polars==1.43.2
polars-runtime-32==1.43.2
prompt_toolkit==3.0.53
propcache==0.5.2
psutil==7.2.2
psycopg==3.2.13
psycopg-binary==3.2.13
pycparser==3.0
pydantic==2.9.2
pydantic_core==2.23.4
pyee==13.0.1
PyJWT==2.13.0
pyparsing==3.3.2
pytest==8.2.2
pytest-django==4.8.0
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
PyYAML==6.0.3
# pyzbar/qrdet/qreader: apps/bot/qr.py's receipt-photo QR decoder (see that
# module's docstring). qreader's YOLO detector (qrdet, via ultralytics/
# torch) locates the QR in the frame; pyzbar decodes the crop it produces.
pyzbar==0.1.9
qrdet==2.5
qreader==3.16
quadrilateral-fitter==1.12
redis==5.0.8
requests==2.34.2
ruff==0.5.7
scipy==1.18.0
setuptools==84.0.0
shapely==2.1.2
six==1.17.0
sqlparse==0.5.5
sympy==1.14.0
# CPU builds specifically — see backend/Dockerfile's --extra-index-url,
# which is what makes pip resolve these (rather than the much larger
# default CUDA wheels) from https://download.pytorch.org/whl/cpu. Both
# MUST stay pinned to a matching +cpu build together: a CPU torch paired
# with a default-index torchvision is an ABI mismatch that fails at QR
# decode time, not at import time (verified — see that Dockerfile comment).
torch==2.13.0+cpu
torchvision==0.28.0+cpu
tqdm==4.70.0
types-PyYAML==6.0.12.20260724
typing_extensions==4.16.0
tzdata==2026.3
ultralytics==8.4.123
ultralytics-platform==0.1.7
ultralytics-thop==2.1.6
urllib3==2.7.0
uvicorn==0.52.1
uvloop==0.22.1
vine==5.1.0
watchfiles==1.2.0
wcwidth==0.8.2
websockets==17.0.1
yarl==1.24.5
